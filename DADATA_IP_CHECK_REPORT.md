# Отчет о проверке Dadata API для Индивидуального предпринимателя (ИП)

## Дата проверки: 2025-12-05

## Результаты проверки

### ✅ Поиск ИП по ИНН (12 цифр)
- **Статус**: ✅ Работает корректно
- **Тестовый ИНН**: `500100732259`
- **Результат**: Найден ИП "Индивидуальный предприниматель Мясина Елена Анатольевна"
- **Тип в ответе**: `INDIVIDUAL`
- **ОГРНИП**: `304500116000157`

### ✅ Валидация формата
- **ИНН ИП (12 цифр)**: ✅ Валидация работает
- **ИНН организации (10 цифр)**: ✅ Валидация работает
- **ОГРНИП (13 цифр)**: ✅ Валидация работает
- **ОГРН организации (13 цифр)**: ✅ Валидация работает

### ✅ Бэкенд обработка
- **Валидация формата**: Код правильно проверяет ИНН (10 или 12 цифр) и ОГРН/ОГРНИП (13 цифр)
- **Тип организации**: Поле `type` в ответе Dadata содержит `"INDIVIDUAL"` для ИП
- **ОГРН/ОГРНИП**: Поле `ogrn` в ответе содержит ОГРНИП для ИП

## Найденные особенности

### 1. Тип организации в Dadata API
Для ИП Dadata API возвращает:
- `type: "INDIVIDUAL"` - указывает на индивидуального предпринимателя
- `ogrn` - содержит ОГРНИП (13 цифр)
- `inn` - содержит ИНН (12 цифр)
- `kpp` - отсутствует (у ИП нет КПП)

### 2. Обработка на фронтенде
В коде фронтенда:
- Функция `extractOrganizationData` извлекает `ogrn` из ответа
- Для ИП это поле должно заполняться в `client_ogrnip`
- Нужно проверить, правильно ли заполняется поле `client_ogrnip` при выборе ИП

### 3. Автозаполнение для ИП
В `CreateContract.vue` строка 1885:
```typescript
} else if (clientType === CLIENT_TYPES.INDIVIDUAL_ENTREPRENEUR) {
  // Для ИП: 12 цифр ИНН или 13 ОГРНИП (поиск не выполняем)
```
**Комментарий указывает, что поиск не выполняется для ИП**, но это может быть проблемой, так как пользователь может ввести ИНН ИП и ожидать автозаполнения.

## Рекомендации

### 1. Включить автозаполнение для ИП
Рекомендуется включить поиск по ИНН/ОГРНИП для ИП, так как:
- Dadata API поддерживает поиск ИП
- Пользователи ожидают автозаполнения при вводе ИНН ИП
- Это улучшит UX

### 2. Правильная обработка ОГРНИП
Убедиться, что при выборе ИП:
- Поле `client_ogrnip` заполняется из `ogrn` ответа Dadata
- Поле `client_ogrn` не заполняется для ИП (или заполняется только для организаций)

### 3. Обработка типа организации
Добавить проверку типа организации (`type === "INDIVIDUAL"`) для:
- Правильного заполнения полей формы
- Отображения соответствующих полей (скрыть КПП для ИП)
- Валидации данных

## Тестовые команды

### Проверка ИП по ИНН:
```bash
cd /Users/com/backend_axenta
go run cmd/test_dadata_ip/main.go
```

## Заключение

✅ **Dadata API работает корректно для ИП**
- Поиск по ИНН ИП (12 цифр) работает
- Валидация формата работает правильно
- Бэкенд правильно обрабатывает запросы

⚠️ **Требуется проверка на фронтенде:**
- Автозаполнение для ИП может быть отключено
- Нужно убедиться, что `client_ogrnip` заполняется правильно
- Рекомендуется включить поиск по ИНН/ОГРНИП для ИП
